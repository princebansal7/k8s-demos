{
  "name": "stateful-backend",
  "version": "1.0.0",
  "description": "node code which writes to a file in every 10 seconds (stateful application)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "princebansal",
  "license": "ISC"
}
